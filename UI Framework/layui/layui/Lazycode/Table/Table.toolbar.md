# Table.toolbar

- 表格头部工具栏

```js
layui.table.on("toolbar(Demoitems)", function(obj) {
  // var data = obj.data;
  // console.log(obj);
  if (obj.event === "123") {
  } else if (obj.event === "Demoitems_refresh") {
    Fun_Demoitems(item);
  } else if (obj.event === "Demoitems_add") {
  } else if (obj.event === "Demoitems_del") {
  } else if (obj.event === "000") {
  }
});
```
